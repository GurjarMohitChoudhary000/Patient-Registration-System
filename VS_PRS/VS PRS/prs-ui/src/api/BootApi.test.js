import { render } from "@testing-library/react";
import React from "react";
import { base_url } from "./BootApi";

describe("base_url", () => {
    it("", () => {
        render(<base_url />);
    });
})