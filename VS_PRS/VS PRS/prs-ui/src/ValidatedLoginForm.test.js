import { render } from "@testing-library/react";
import React from "react";
import ValidatedLoginForm from "./ValidatedLoginForm";

describe("ValidatedLoginForm", () => {
    it("", () => {
        render(<ValidatedLoginForm />);
    });
})