import { render } from "@testing-library/react";
import React from "react";
import AddRoom from "./AddRoom";

describe("AddRoom", () => {
    it("", () => {
        render(<AddRoom />);
    });
})